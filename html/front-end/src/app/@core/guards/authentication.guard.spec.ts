import { Authentication.Guard } from './authentication.guard';

describe('Authentication.Guard', () => {
  it('should create an instance', () => {
    expect(new Authentication.Guard()).toBeTruthy();
  });
});
